# URL base do backend (opcional). Se não for definida, os recursos de
# conta, sincronização e compartilhamento ficam ocultos e o app funciona
# 100% localmente (gravação, biblioteca, download e exclusão continuam
# funcionando normalmente sem isso).
# Exemplo em produção: VITE_API_URL=https://api.seu-dominio.com
VITE_API_URL=
